[build]
dockerfilePath = "Dockerfile"
watchPatterns = ["backend/**", "migration/**", "Dockerfile", "run_migrations.sh"]

[deploy]
startCommand = "/app/run_migrations.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
